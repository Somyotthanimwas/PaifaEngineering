services:
  - type: web
    runtime: static
    name: PaifaEngineering
    buildCommand: pnpm install && pnpm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
